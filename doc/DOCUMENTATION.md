# Documentation

```txt
Usage:  poe [FLAGS] [FILES:LINE:COL:MODE...]

Philzon's Own Editor

Modes:
  a   append data to current buffer
  i   enable insert mode
  r   enable read-only mode

Options:
  -h, --help      print this and exit
  -v, --version   print version and exit
```
